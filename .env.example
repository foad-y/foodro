# Supabase settings (replace with your project's values)
VITE_API_BASE_URL=http://localhost:3030/api


# For local quick login (dev-only):
# If you don't set the two variables above, the app provides a local test account:
# email: admin@local
# password: password
